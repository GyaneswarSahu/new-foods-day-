$(document).ready(function() {
    // Example of updating order count dynamically
    const orderCount = 5; // Fetch this value from the server in a real application
    $('.order-count').text(orderCount);

    // Add any additional JS functionality for the admin panel here
});
